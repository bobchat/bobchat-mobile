import React, { Component } from 'react';
import { Icon } from 'react-native-elements';

export default class BackButton extends Component {
  render() {
    return <Icon name='chevron-left' size={25} />;
  }
}
